FLASK_APP=app:create_app
FLASK_ENV=development
SECRET_KEY=changeme
SECURITY_PASSWORD_SALT=changeme
DATABASE_URL=sqlite:////data/app.db
UPLOAD_FOLDER=/data/uploads
PORT=8080
DB_PORT=5432
# For Postgres instead of SQLite, override DATABASE_URL accordingly (postgresql://...)
# Mail settings (set MAIL_DEV_LOG_ONLY=true to log verification links instead of sending)
MAIL_SERVER=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_DEFAULT_SENDER=
MAIL_DEV_LOG_ONLY=true
APP_BASE_URL=http://localhost:8080
